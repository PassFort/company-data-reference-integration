apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: integrations-duedil
spec:
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 50%
  selector:
    matchLabels:
      passfort: integrations-duedil
  progressDeadlineSeconds: 240
  template:
    metadata:
      name: integrations-duedil
      labels:
        passfort: integrations-duedil
    spec:
      containers:
      - name: integrations-duedil
        image: {container_uri}
        resources:
          requests:
            cpu: "20m"
            memory: "256Mi"
        ports:
        - containerPort: 8001
        env:
        - name: CLUSTER
          value: {cluster}
        - name: SENTRY_URL
          value: {sentry_url}
        readinessProbe:
          httpGet:
            path: '/health'
            port: 8001

---

apiVersion: v1
kind: Service
metadata:
  name: integrations-duedil
spec:
  ports:
    - name: http
      port: 8001
  selector:
    passfort: integrations-duedil

apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: integrations-base
spec:
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 50%
  selector:
    matchLabels:
      passfort: integrations-base
  progressDeadlineSeconds: 240
  template:
    metadata:
      name: integrations-base
      labels:
        passfort: integrations-base
    spec:
      containers:
      - name: integrations-base
        image: {container_uri}
        ports:
        - containerPort: 8001
        env:
        - name: CLUSTER
          value: {cluster}
        - name: SENTRY_URL
          value: {sentry_url}
        readinessProbe:
          httpGet:
            path: '/health'
            port: 8001

---

apiVersion: v1
kind: Service
metadata:
  name: integrations-base
spec:
  ports:
    -
      port: 8001
  selector:
    passfort: integrations-base
